# Store modules in node_modules
node-linker=hoisted

# Enable strict peer dependencies
strict-peer-dependencies=false

# Set registry timeout (helps with network issues)
fetch-timeout=60000

# Optional: Use shamefully-hoist for better compatibility
shamefully-hoist=true