---
const audiences = [
  {
    title: 'Desenvolvedores Front-end',
    description:
      'Aprenda a criar interfaces acessíveis desde o início do desenvolvimento, implementando as melhores práticas de HTML semântico, WAI-ARIA e JavaScript acessível.',
    benefits: [
      'Domínio de HTML semântico e WAI-ARIA',
      'Implementação de componentes acessíveis',
      'Técnicas avançadas de desenvolvimento inclusivo',
      'Melhores práticas de JavaScript para acessibilidade',
    ],
  },
  {
    title: 'Analistas de Qualidade',
    description:
      'Desenvolva habilidades para testar e garantir a acessibilidade em aplicações web, utilizando ferramentas automatizadas e realizando testes manuais eficientes.',
    benefits: [
      'Domínio de ferramentas de teste de acessibilidade',
      'Técnicas de teste manual com leitores de tela',
      'Criação de planos de teste focados em acessibilidade',
      'Identificação e documentação de problemas',
    ],
  },
];
---

<section id="target-audience" class="bg-gray-900 py-16 md:py-24" aria-labelledby="target-heading">
  <div class="container mx-auto px-6">
    <div class="mb-16 text-center">
      <span
        class="text-blood-red-800 mb-4 inline-block rounded-full bg-white px-4 py-1 text-sm font-medium"
      >
        Público-Alvo
      </span>
      <h2 id="target-heading" class="mb-6 text-3xl font-bold text-white md:text-4xl">
        Para quem é a <span class="text-vistaBlue">mentoria</span>?
      </h2>
      <p class="mx-auto max-w-3xl text-lg text-white md:text-xl">
        Um programa desenvolvido para profissionais que desejam aprender acessibilidade na prática.
      </p>
    </div>

    <div class="mb-16 grid grid-cols-1 gap-8 md:grid-cols-2">
      {
        audiences.map((audience) => {
          return (
            <div class="border-blood-red-800/20 rounded-lg border bg-gray-100 p-8">
              <h3 class="text-errieBlack mb-4 text-2xl font-bold">{audience.title}</h3>
              <p class="text-errieBlack/80 mb-6">{audience.description}</p>
              <ul class="space-y-3">
                {audience.benefits.map((benefit) => (
                  <li class="text-errieBlack/90 flex items-center">
                    <div class="bg-blood-red-800 mr-3 h-1.5 w-1.5 rounded-full" />
                    {benefit}
                  </li>
                ))}
              </ul>
            </div>
          );
        })
      }
    </div>
  </div>
</section>
