---
import faqs from '@data/faq.json';
import { ArrowRight } from '@lucide/astro';
---

<section class="bg-gray-50 py-24" aria-labelledby="faq-title">
  <div class="mx-auto max-w-4xl px-6">
    <div class="mb-16 text-center">
      <h2 class="text-errie-black-900 mb-4 text-4xl font-bold" id="faq-title">
        Perguntas Frequentes
      </h2>
      <p class="text-errie-black-900 text-xl">
        Ainda com dúvidas? Aqui estão as respostas mais comuns
      </p>
    </div>

    <div class="space-y-6">
      {
        faqs.map((faq) => (
          <article class="rounded-xl border border-gray-200 bg-white p-6 shadow-sm">
            <h3 class="text-errie-black-900 mb-3 text-lg font-bold">{faq.question}</h3>
            <p class="text-errie-black-900 leading-relaxed">{faq.answer}</p>
          </article>
        ))
      }
    </div>

    <div class="mt-12 text-center">
      <p class="text-errie-black-900 mb-4">Não encontrou sua dúvida?</p>
      <a
        href="/contato"
        class="text-blood-red-800 inline-flex items-center gap-2 font-semibold hover:underline hover:underline-offset-8"
      >
        Me manda uma mensagem
        <ArrowRight class="h-4 w-4" aria-hidden="true" />
      </a>
    </div>
  </div>
</section>
