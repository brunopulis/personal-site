---
const supporters = [
  { name: 'João Silva', platform: 'Ko-fi' },
  { name: 'Maria Santos', platform: 'Liberapay' },
  { name: 'Pedro Oliveira', platform: 'Pix' },
  { name: 'Ana Costa', platform: 'PayPal' },
  { name: 'Carlos Mendes', platform: 'Ko-fi' },
  { name: 'Julia Rocha', platform: 'Liberapay' },
];
---

{
  supporters.length > 0 && (
    <div class="mt-16 border-t-2 border-gray-200 pt-12">
      <h2 class="text-2xl font-bold text-center mb-8 text-gray-900">
        Agradecimentos especiais aos apoiadores 🌟
      </h2>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
        {supporters.map((supporter) => (
          <div
            key={supporter.name}
            class="bg-white border border-gray-200 rounded-lg p-4 flex items-center justify-between hover:shadow-md transition-shadow"
          >
            <div>
              <p class="font-semibold text-gray-900">{supporter.name}</p>
              <p class="text-sm text-gray-500">via {supporter.platform}</p>
            </div>
            <span class="text-2xl">❤️</span>
          </div>
        ))}
      </div>
    </div>
  )
}
