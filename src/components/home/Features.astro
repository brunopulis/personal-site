---
import { Users, Presentation, ArrowRight, Code,MessageCircle, PersonStanding } from "@lucide/astro";
import { Image } from "astro:assets";

import mentoria  from "@/assets/images/site/mentoria.webp";
import auditoria from "@/assets/images/site/auditoria.webp";
import palestras  from "@/assets/images/site/palestras.webp";

const icons = { Users, Code, MessageCircle };

const features = [
  {
    title: "Mentoria",
    description: "Não adianta só entregar um relatório. Eu trabalho COM você ou sua equipe para implementar acessibilidade de verdade no dia a dia.",
    icon: "Users",
    imageUrl: mentoria,
    chipClass: "inline-block bg-green-100 text-green-700 text-xs font-semibold px-3 py-1 rounded-full mb-4",
    chip: "Mais procurado",
    url: "/servicos/mentorias-consultorias",
  },
  {
    title: "Auditoria",
    description:
      "Analiso seu site ou documento e te mostro exatamente o que precisa melhorar (e por quê isso importa para seus usuários).",
    icon: "Code",
    imageUrl: auditoria,
    chipClass: "inline-block bg-blue-100 text-blue-700 text-xs font-semibold px-3 py-1 rounded-full mb-4",
    chip: "Começo ideal",
    url: "/servicos/auditoria-de-acessibilidade",
  },
  {
    title: "Palestras e Treinamentos",
    description:
      "Levo acessibilidade para empresas e eventos de forma prática, sem jargões complicados.",
    icon: "MessageCircle",
    imageUrl: palestras,
    chipClass: "inline-block bg-purple-100 text-purple-700 text-xs font-semibold px-3 py-1 rounded-full mb-4",
    chip: "Inspirador",
    url: "/servicos/palestras-treinamentos",
  }
];
---

<section class="py-24 bg-gray-100">
  <div class="max-w-6xl mx-auto px-6">
    <div class="text-center mb-16">
      <h2 class="text-4xl font-bold text-gray-900 mb-4">
        Como posso te ajudar
      </h2>
      <p class="text-xl text-errie-black-600">
        Três formas principais de trabalharmos juntos
      </p>
    </div>

    <div class="grid md:grid-cols-3 gap-8">
      {
        features.map((feature, index) => (
          <div class="bg-white p-8 rounded-2xl shadow-sm hover:shadow-xl transition group">
            <div class="mb-6">
              <Image
                src={feature.imageUrl}
                alt=""
                class="w-full h-48 object-cover rounded-xl"
                loading="lazy"
              />
            </div>
            <div class="flex items-center gap-3 mb-4">
              <div class="w-10 h-10 bg-red-100 rounded-lg flex items-center justify-center">
                {icons[feature.icon] && <icons[feature.icon] class="h-5 w-5" aria-hidden="true" />}
              </div>
              <h3 class="text-2xl font-bold text-gray-900">
                {feature.title}
              </h3>
            </div>
            <p class="text-errie-black-600 mb-4">
              {feature.description}
            </p>
            <span class={feature.chipClass}>
              {feature.chip}
            </span>
            <a href={feature.url} class="text-blood-red-800 font-semibold flex items-center gap-2 group-hover:gap-3 transition-all">
              Saber mais
              <ArrowRight class="w-4 h-4" />
            </a>
          </div>
        ))
      }
    </div>

    <div class="text-center pt-8">
      <a href="/servicos" class="inline-flex items-center gap-2 font-semibold text-blood-red-800 hover:underline">
        Explorar todos os serviços
        <ArrowRight class="h-4 w-4" aria-hidden="true" />
      </a>
    </div>
  </div>
</section>
