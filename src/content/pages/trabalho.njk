---
layout: page
title: "Meus trabalhos"
permalink: "/trabalho/index.html"
description: "Projetos e trabalhos desenvolvidos com foco em acessibilidade, performance e experiência do usuário."
---

{% set projects = portfolio.projects %}
{% set categories = portfolio.categories %}
{% set stats = portfolio.stats %}

<section class="py-5">
  <div class="container">
    {# Filtros (Opcional) #}
    <div class="row mb-5">
      <div class="col-12">
        <div class="d-flex flex-wrap gap-2 justify-content-center">
          <button class="btn btn-sm btn-outline-dark active" data-filter="all">
            Todos
          </button>
          <button class="btn btn-sm btn-outline-dark" data-filter="web">
            Sites
          </button>
          <button class="btn btn-sm btn-outline-dark" data-filter="app">
            Aplicações
          </button>
          <button class="btn btn-sm btn-outline-dark" data-filter="a11y">
            Acessibilidade
          </button>
          <button class="btn btn-sm btn-outline-dark" data-filter="opensource">
            Open Source
          </button>
        </div>
      </div>
    </div>

    <div class="row g-4" id="portfolio-grid">
      {% for project in projects %}
      <div class="col-12 col-sm-6 col-lg-4 portfolio-item" data-category="{{ project.category | join(' ') }}"
        data-year="{{ project.year }}">
        <div class="card h-100 border-0 shadow-sm portfolio-card">
          <img src="{{ project.image }}" class="card-img-top" alt="{{ project.imageAlt }}" loading="lazy">
          {% if project.featured %}
          <div class="position-absolute top-0 end-0 m-2">
            <span class="badge bg-warning text-dark">
              <i class="fas fa-star"></i> Destaque
            </span>
          </div>
          {% endif %}

          <div class="card-body d-flex flex-column">
            <h3 class="h5 card-title fw-bold">{{ project.title }}</h3>
            <p class="card-text text-muted flex-grow-1">
              {{ project.description }}
            </p>

            {# Technologies #}
            <div class="d-flex gap-2 flex-wrap mb-3">
              {% for tech in project.technologies %}
              <span class="badge bg-secondary">{{ tech }}</span>
              {% endfor %}
            </div>

            {# Client/Year #}
            {% if project.client or project.year %}
            <p class="small text-muted mb-3">
              {% if project.client %}
              <i class="fas fa-briefcase me-1"></i> {{ project.client }}
              {% endif %}
              {% if project.year %}
              <span class="ms-2">
                <i class="fas fa-calendar me-1"></i> {{ project.year }}
              </span>
              {% endif %}
            </p>
            {% endif %}

            {# Links #}
            <div class="d-flex gap-2 flex-wrap">
              {% if project.links.demo %}
              <a href="{{ project.links.demo }}" target="_blank" rel="noopener noreferrer"
                class="btn btn-sm btn-blood-red-800 text-white flex-fill">
                <i class="fas fa-external-link-alt me-1"></i> Ver projeto
              </a>
              {% endif %}

              {% if project.links.github %}
              <a href="{{ project.links.github }}" target="_blank" rel="noopener noreferrer"
                class="btn btn-sm btn-outline-dark" title="Ver no GitHub">
                <i class="fab fa-github"></i>
              </a>
              {% endif %}

              {% if project.links.case %}
              <a href="{{ project.links.case }}" class="btn btn-sm btn-outline-dark" title="Ver case completo">
                <i class="fas fa-book"></i>
              </a>
              {% endif %}
            </div>
          </div>
        </div>
      </div>
      {% endfor %}
    </div>
  </div>
</section>

{# CTA Section #}
<section class="bg-light py-5">
  <div class="container">
    <div class="row">
      <div class="col-lg-8 mx-auto text-center">
        <h2 class="h3 fw-bold mb-3">Gostou do que viu?</h2>
        <p class="lead text-muted mb-4">
          Estou disponível para novos projetos e colaborações.
        </p>
        <div class="d-flex gap-3 justify-content-center flex-wrap">
          <a href="/contato/" class="btn btn-primary text-white btn-lg">
            <i class="fas fa-envelope me-2"></i>
            Entre em contato
          </a>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  // Filtro de portfólio
  document.addEventListener('DOMContentLoaded', () => {
    const filterButtons = document.querySelectorAll('[data-filter]');
    const portfolioItems = document.querySelectorAll('.portfolio-item');

    filterButtons.forEach(button => {
      button.addEventListener('click', () => {
        const filter = button.dataset.filter;

        // Atualiza botões ativos
        filterButtons.forEach(btn => btn.classList.remove('active'));
        button.classList.add('active');

        // Filtra itens
        portfolioItems.forEach(item => {
          if (filter === 'all' || item.dataset.category.includes(filter)) {
            item.style.display = 'block';
            item.classList.add('fade-in');
          } else {
            item.style.display = 'none';
          }
        });
      });
    });
  });
</script>