---
layout: page
title: "Notas"
description: "Pensamentos curtos, links interessantes e atualizações rápidas."
permalink: "{% if pagination.pageNumber > 0 %}/notas/page/{{ pagination.pageNumber + 1 }}/{% else %}/notas/{% endif %}"
pagination:
  data: collections.notes
  size: 12
---

<div class="max-w-[600px] mx-auto px-6 pb-24 space-y-6">
  {% for note in pagination.items %}
  <article class="bg-brand-dark-900 prose prose-lg border border-brand-gray-200/30 rounded-xl p-5 shadow-sm">
    <time datetime="{{ note.data.pubDate | toISOString }}" class="dt-published text-brand-gray-500">
      {{ note.data.pubDate | formatDate('DD/MM/YYYY') }}
    </time>

    <div class="note-content prose prose-link:text-brand-blue-500 text-brand-gray-500 mb-6">{{ note.content | safe }}</div>

    <div class="border-t border-brand-gray-200/30 pt-4">
      {% if note.data.tags and note.data.tags.length > 0 %}
      {% for tag in note.data.tags %}
      {% if tag !== "notes" %}
      <span class="p-category text-brand-blue-500 tags">#{{ tag }}</span>
      {% endif %}
      {% endfor %}
      {% endif %}
    </div>
  </article>
  {% endfor %}
</div>
</div>

{% include 'components/pagination.njk' %}