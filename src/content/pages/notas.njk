---
layout: page
title: "Notas"
description: "Pensamentos curtos, links interessantes e atualizações rápidas."
permalink: "{% if pagination.pageNumber > 0 %}/notas/page/{{ pagination.pageNumber + 1 }}/{% else %}/notas/{% endif %}"
pagination:
  data: collections.notes
  size: 12
---

<div class="grid grid-cols-1 lg:grid-cols-2 gap-4">
  {% for note in pagination.items %}
  <article class="bg-brand-dark-900 border border-brand-gray-200/30 rounded-2xl p-6 shadow-sm hover:shadow-md transition-shadow flex flex-col">
    <time datetime="{{ note.data.pubDate | toISOString }}" class="dt-published text-xs font-semibold tracking-widest uppercase text-brand-gray-500 mb-4">
      {{ note.data.pubDate | formatDate('DD/MM/YYYY') }}
    </time>

    <div class="note-content prose prose-invert prose-brand-blue max-w-none text-brand-gray-300 flex-grow">
      {{ note.content | safe }}
    </div>

    {% if note.data.tags and note.data.tags.length > 0 %}
    <div class="border-t border-brand-gray-200/10 mt-6 pt-4 flex flex-wrap gap-2">
      {% for tag in note.data.tags %}
        {% if tag !== "notes" %}
        <span class="p-category text-xs font-bold text-brand-blue-500/80 hover:text-brand-blue-500 transition-colors cursor-default">#{{ tag }}</span>
        {% endif %}
      {% endfor %}
    </div>
    {% endif %}
  </article>
  {% endfor %}
</div>

{% include 'components/pagination.njk' %}