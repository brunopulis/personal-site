---
layout: page
title: "Notas"
description: "Posts curtos no estilo do Twitter, pensamentos e qualquer coisa."
permalink: "{% if pagination.pageNumber > 0 %}/notas/page/{{ pagination.pageNumber + 1 }}/{% else %}/notas/{% endif %}"
pagination:
  data: collections.notes
  size: 12
---

<div class="flow region">
  {% for note in pagination.items %}
    <article class="card">
      <time datetime="{{ note.data.pubDate | toISOString }}" class="dt-published">
        {{ note.data.pubDate | formatDate('DD/MM/YYYY') }}
      </time>
      
      <p class="flow">{{ note.content | safe }}</p>
      
      <div class="flow">
        {% if note.data.tags and note.data.tags.length > 0 %}
          {% for tag in note.data.tags %}
            {% if tag !== "notes" %}
              <span class="p-category tags">#{{ tag }}</span>
            {% endif %}
          {% endfor %}
        {% endif %}
      </div>
    </article>
    {% endfor %}
  </div>
</div>

{% include 'components/pagination.njk' %}
