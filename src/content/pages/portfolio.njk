---
layout: page
title: "Meus trabalhos"
permalink: "/portfolio/index.html"
description: "Uma seleção de projetos variando de desenvolvimento front-end ao design de interface, focada em
performance e usabilidade."
---

{% set projects = portfolio.projects %}
{% set categories = portfolio.categories %}
{% set stats = portfolio.stats %}

<section class="region flow">
  <h2 class="title p-name">Projetos concluídos</h2>
  <div class="grid" data-layout="50-50" id="portfolio-grid">
    {% for project in projects %}
    <article class="flow h-entry">
      <img src="{{ project.image }}" class="portfolio__image u-photo" alt="{{ project.imageAlt }}" loading="lazy">
      <div class="flow">
        <h3>
          <a href="{{ project.links.demo }}">{{ project.title }}</a>
        </h3>
        <p>{{ project.description }}</p>

        <ul class="portfolio__technologies" role="list">
          {% for tech in project.technologies %}
          <li class="badge bg-secondary">{{ tech }}</li>
          {% endfor %}
        </ul>
      </div>
    </article>
    {% endfor %}
  </div>
</section>

<section class="region">
  <div class="box flow">
    <h2 class="title">Vamos construir algo juntos?</h2>
    <p>
      Estou sempre aberto a novos projetos, mentorias e colaborações interessantes em engenharia de software e design.
    </p>
    <nav class="cluster">
      <a href="/contato/" class="btn btn-primary text-white btn-lg">
        Entre em contato
      </a>
      <a href="/cv/" class="btn btn-primary text-white btn-lg">
        Veja meu currículo
      </a>
    </nav>
  </div>
</section>

<script>
  // Filtro de portfólio
  document.addEventListener('DOMContentLoaded', () => {
    const filterButtons = document.querySelectorAll('[data-filter]');
    const portfolioItems = document.querySelectorAll('.portfolio-item');

    filterButtons.forEach(button => {
      button.addEventListener('click', () => {
        const filter = button.dataset.filter;

        // Atualiza botões ativos
        filterButtons.forEach(btn => btn.classList.remove('active'));
        button.classList.add('active');

        // Filtra itens
        portfolioItems.forEach(item => {
          if (filter === 'all' || item.dataset.category.includes(filter)) {
            item.style.display = 'block';
            item.classList.add('fade-in');
          } else {
            item.style.display = 'none';
          }
        });
      });
    });
  });
</script>