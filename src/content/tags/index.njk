---
layout: base
title: Tags
permalink: /tags/
---
<h1 class="mb-6 font-bold text-2xl">Tags</h1>

{% if collections.tagList and collections.tagList.length %}
<ul class="gap-3 grid grid-cols-1 sm:grid-cols-2">
  {% for t in collections.tagList %}
    {% set count = 0 %}
    {% for post in collections.posts %}
      {% if post.data.tags and (t in post.data.tags) %}
        {% set count = count + 1 %}
      {% endif %}
    {% endfor %}
    <li class="flex justify-between items-center hover:bg-gray-50 dark:hover:bg-gray-800 p-3 border rounded">
      <a class="font-medium hover:text-blue-600" href="/tags/{{ t | slug }}/">#{{ t }}</a>
      <span class="text-gray-500 text-xs">{{ count }} post{% if count != 1 %}s{% endif %}</span>
    </li>
  {% endfor %}
</ul>
{% else %}
  <p class="text-gray-500">No tags yet.</p>
{% endif %}