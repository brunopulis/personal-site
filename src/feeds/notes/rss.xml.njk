---json
{
  "permalink": "/feeds/notes/rss.xml",
  "eleventyExcludeFromCollections": true,
  "metadata": {
  "title": "Bruno Pulis - Notas",
    "description": "Posts curtos, estilo twitter, pensamentos e qualquer comentário aleatório",
    "language": "pt-br",
    "base": "https://www.brunopulis.com",
    "author": {
      "name": "Bruno Pulis",
      "email": "brunopulis@protonmail.com"
    }
  }
}
---
<?xml version="1.0" encoding="utf-8"?>
<?xml-stylesheet href="/feeds/pretty-feed-v3.xsl" type="text/xsl"?>
<rss version="2.0" xmlns:dc="http://purl.org/dc/elements/1.1/" xml:base="{{ metadata.base | addPathPrefixToFullUrl }}"
  xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ metadata.title }}</title>
    <link>{{ metadata.base | addPathPrefixToFullUrl }}</link>
    <atom:link href="{{ permalink | htmlBaseUrl(metadata.base) }}" rel="self" type="application/rss+xml" />
    <description>{{ metadata.description }}</description>
    <language>{{ metadata.language or page.lang }}</language>
    {%- set notes = collections.notes %}
    {%- for note in notes | reverse | limit(10) %}
    {%- set absolutePostUrl = note.url | htmlBaseUrl(metadata.base) %}
    <item>
      <title>{{ note.data.title or (note.data.pubDate or note.date) | toRfc822Date }}</title>
      <link>{{ absolutePostUrl }}</link>
      <description>{{ note.content | renderTransforms(note.data.page, metadata.base) }}</description>
      <pubDate>{{ (note.data.pubDate or note.date) | toRfc822Date }}</pubDate>
      <dc:creator>{{ metadata.author.name }}</dc:creator>
      <guid>{{ absolutePostUrl }}</guid>
      {%- if note.data.tags %}
      {%- for tag in note.data.tags %}
      {%- if tag not in ["posts", "all"] %}
      <category>{{ tag }}</category>
      {%- endif %}
      {%- endfor %}
      {%- endif %}
    </item>
    {%- endfor %}
  </channel>
</rss>