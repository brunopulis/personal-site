<section class="pb-12">
  <div class="max-w-7xl mx-auto max-sm:px-6">
    <div class="flex flex-col md:flex-row items-start md:items-center md:mb-4 justify-between gap-4">
      <h2 class="text-errie-black-900 font-bold text-4xl mb-4">Últimos artigos</h2>
      <a href="/blog" class="underline underline-offset-8">todos artigos</a>
    </div>

    {% set allRecentPosts = collections.posts %}
    {% if allRecentPosts.length %}
    <ul class="grid gap-6 md:grid-cols-1">
      {% for post in allRecentPosts.slice(0, 6) %}
      <li class="py-4">
        <h3 class="mb-4 font-bold text-3xl">
          <a href="{{ post.url }}"
            class="text-errie-black-900 font-semibold flex gap-2 items-center transition underline underline-offset-8 hover:no-underline">
            {{ post.data.title }}
          </a>
        </h3>

        {% if post.data.description %}
        <p class="flex items-center font-light gap-4 my-4 text-xl">{{ post.data.description }}</p>
        {% endif %}
        <time datetime="{{ post.data.pubDate }}" class="text-errie-black-900 font-display">
          Publicado em: {{ post.data.pubDate | formatDate('DD/MM/YYYY') }}
        </time>
      </li>
      {% endfor %}
    </ul>
    {% else %}
    <p class="text-errie-black-900 font-display text-center">Nenhuma postagem encontrada.</p>
    {% endif %}
  </div>
</section>