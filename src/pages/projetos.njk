---
layout: base
title: "Meus trabalhos"
permalink: "/projetos/index.html"
date: Last Modified
description: "Uma seleção de projetos variando de desenvolvimento front-end ao design de interface, focada em
performance e usabilidade."
---

{% set projects = portfolio.projects %}
{% set stats = portfolio.stats %}

<div class="wrapper region">
  <header class="flow">
    <h1 class="p-name">{{ title }}</h1>
    <p>{{ description }}</p>
  </header>

  <div class="h-feed region flow">
    <ul class="grid" data-layout="50-50" role="list">
      {% for project in projects %}
      <li class="card">
        <img alt="{{ project.imageAlt or project.title }}" class="w-full h-full object-cover" src="{{ project.image }}">
        <div class="flow">
          <h2>
            <a href="{{ project.url or project.links.demo }}">{{ project.title }}</a>
          </h2>
          <p>
            {{ project.description }}
          </p>
          <a href="{{ project.url or project.links.demo }}">
            visualizar projeto
            <span class="visually-hidden">{{ project.title }}</span>
          </a>
        </div>
      </li>
      {% endfor %}
    </ul>
  </div>

  <article class="card">
    <div class="flow">
      <h2>Vamos construir algo juntos?</h2>
      <p>Estou sempre aberto a novos projetos, mentorias e colaborações interessantes em engenharia de
        software e design.
      </p>

      <div class="cluster | repel">
        <div>
          <a href="/contact">Entre em contato</a>
          <a href="/resume">Veja meu currículo</a>
        </div>
      </div>
    </div>
  </article>
</div>