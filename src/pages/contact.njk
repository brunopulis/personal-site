---
layout: base
title: "Contato"
description: Formas de entrar em contato comigo
permalink: "/contact/index.html"
date: Last Modified
---

<h1 class="text-2xl font-bold mb-6">Entre em contato</h1>

<p>Eu adoraria ouvir sua voz! Se você tiver alguma dúvida, feedback ou apenas quiser cumprimentar,
  sinta-se à vontade para me contatar por qualquer um dos canais abaixo.</p>

<div class="grid grid-cols-1 md:grid-cols-2 gap-8 mt-8">
  <div>
    <h2>Entrar em contato</h2>
    <ul class="space-y-3 mt-4">
      <li class="flex items-center gap-3">
        <span class="text-blue-600">📧</span>
        <a href="mailto:{{ site.email }}" class="hover:text-blue-600">{{ site.email }}</a>
      </li>
      {% if site.social.github %}
      <li class="flex items-center gap-3">
        <span class="text-blue-600">🐙</span>
        <a href="https://github.com/{{ site.social.github }}" target="_blank" rel="noopener"
          class="hover:text-blue-600">GitHub</a>
      </li>
      {% endif %}
      {% if site.social.mastodon %}
      <li class="flex items-center gap-3">
        <span class="text-blue-600">🐘</span>
        <a href="{{ site.social.mastodon | replace('@', 'https://') | replace('@', '/') }}" target="_blank"
          rel="noopener" class="hover:text-blue-600">Mastodon</a>
      </li>
      {% endif %}
    </ul>
  </div>

  <div>
    <h2 class="text-xl font-semibold mb-4">Mensagem rápida</h2>
    <form class="space-y-4" action="#" method="POST">
      <div>
        <label for="name" class="block text-sm font-medium mb-1">Nome</label>
        <input type="text" id="name" name="name" required
          class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" />
      </div>
      <div>
        <label for="email" class="block text-sm font-medium mb-1">Email</label>
        <input type="email" id="email" name="email" required
          class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500" />
      </div>
      <div>
        <label for="message" class="block text-sm font-medium mb-1">Mensagem</label>
        <textarea id="message" name="message" rows="4" required
          class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500"></textarea>
      </div>
      <button type="submit"
        class="px-6 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500">
        Enviar mensagem
      </button>
    </form>
    <p class="text-sm text-gray-600 dark:text-gray-400 mt-2">
      Nota: Configure com Netlify Forms ou serviço similar para funcionalidade.
    </p>
  </div>
</div>

{% include "components/last-update.njk" %}