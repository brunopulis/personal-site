---
layout: base
title: "Blog"
description: "Escrevo para organizar ideias, questionar práticas comuns da web e compartilhar aprendizados sobre acessibilidade, front-end e pensamento crítico."
permalink: "/blog/{% if pagination.pageNumber > 0 %}page/{{ pagination.pageNumber + 1 }}/{% endif %}"
pagination:
  data: collections.posts
  size: 12
---

<section class="wrapper region">
  <header class="flow">
    <h1>{{ title }}</h1>
    <p>{{ description }}</p>
    <a class="hidden u-url" href="{{ site.url }}/blog/"></a>
  </header>

  <div class="h-feed">
    {% for post in pagination.items %}
    <article class="prose | region flow h-entry">
      <h2 class="p-name">
        <a class="u-url" href="{{ post.url }}">{{
          post.data.title }}</a>
      </h2>
      <time class="dt-published" datetime="{{ post.data.pubDate | toISOString }}">
        Publicado em: {{ post.data.pubDate | readableDate }}
      </time>

      <p class="p-summary">
        {{ post.data.description or (post.templateContent | striptags | truncate(160)) }}
      </p>

      <div clas="tags__container">
        {% for tag in post.data.tags %}
        <a class="p-category tags" href="/tags/{{ tag | slug }}/">#{{ tag }}</a>
        {% endfor %}
      </div>
    </article>
    {% endfor %}
  </div>

  {% if pagination.pages.length > 1 %}
  {% include 'components/pagination.njk' %}
  {% endif %}
</section>