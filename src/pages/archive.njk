---
layout: base
permalink: "/archive/"
title: Arquivo
description: Todos os post, organizados por data de publicação.
date: Last Modified
---

<div class="wrapper region">
  <header class="flow">
    <h1 class="p-name">{{ title }}</h1>
    <p>{{ description }}</p>
  </header>

  {% set postsByYear = collections.posts | groupByYear %}
  {% for year, posts in postsByYear %}
  <section class="region flow">
    <h2>{{ year }}</h2>

    {% set postsByMonth = posts | groupByMonth %}
    {% for month, monthPosts in postsByMonth %}
    <article class="card">
      <div class="flow">
        <h3 class="mb-3 font-medium text-brand-red-500 text-2xl capitalize">
          {{ monthPosts[0].data.pubDate | formatDate("MMMM") }}
        </h3>

        {% for post in monthPosts %}
        <article class="h-entry">
          <time class="font-mono text-sm dt-published">
            {{ post.data.pubDate | formatDate("MMM dd") }}
          </time>
          <div class="flex-1">
            <h4 class="text-xl">
              <a href="{{ post.url }}"
                class="font-bold text-brand-red-500 hover:text-brand-red-600 underline hover:no-underline">
                {{ post.data.title }}
              </a>
            </h4>
            {% if post.data.description %}
            <p class="mt-1 text-sm">{{ post.data.description }}</p>
            {% endif %}
            {% if post.data.tags %}
            <div class="flex flex-wrap gap-1 mt-2">
              {% for tag in post.data.tags %}
              <span class="inline-block px-2 py-1 rounded-full text-white text-xs"
                style="--tag-bg: {{ tag }}; background-color: var(--tag-bg);">
                {{ tag }}
              </span>
              {% endfor %}
            </div>
            {% endif %}
          </div>
        </article>
        {% endfor %}
      </div>
</div>
</div>
{% endfor %}
</section>
{% endfor %}
</div>