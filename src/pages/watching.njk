---
layout: base.njk
title: "Filmes e Séries"
permalink: /watching/
---

<section class="region" style="--region-space: var(--space-l)">
  <h1 class="mb-6 p-name">Filmes e Séries</h1>
  <p class="mb-4">
    Filmes e séries que assisti, apresentados de forma organizada.
  </p>

  {% set allMovies = collections.movies %}
  {% set allShows = collections.shows %}
  {% set allItems = allMovies | concat(allShows) %}
  {% set yearsGrouped = allItems | groupByYear %}
  {% set years = yearsGrouped.years %}
  {% set currentYear = years[0] %}

  {% if years.length > 0 %}
  <div class="cluster watching-years">
    {% for item in years %}
      <a href="/watching/{{ item }}/" data-state="{{ 'active' if item == currentYear }}">
        {{ item }}
      </a>
    {% endfor %}
  </div>
  {% endif %}
</section>

{% set recentMovies = collections.movies | sortByDate | limit(8) %}
{% set recentShows = collections.shows | sortByDate | limit(8) %}
{% set favoriteMovies = collections.movies | filterFavorites | sortByDate | limit(8) %}
{% set favoriteShows = collections.shows | filterFavorites | sortByDate | limit(8) %}

{% if recentMovies.length > 0 %}
<section class="region" style="--region-space: var(--space-2xl)" aria-labelledby="recent-movies">
  <div class="cluster mb-6" style="--gutter: var(--space-xs)">
    <h2 id="recent-movies">
      {% faIcon 'film', 'solid', '1em' %} Filmes Recentes
    </h2>
    <a href="/watching/movies/" class="text-sm" style="color: var(--color-link)">Ver todos →</a>
  </div>

  {% if recentMovies.length > 0 %}
  <div class="grid h-feed" style="--grid-min-item-size: 12rem; --gutter: var(--space-xs)">
    {% for movie in recentMovies %}
      <article class="mb-5 h-entry">
        <a href="{{ movie.url }}" class="block">
          {% if movie.data.poster %}
            <div class="frame" style="--n: 7; --d: 10;">
              <img
                src="{{ movie.data.poster | localPoster }}"
                alt=""
                loading="lazy"
              >
            </div>
          {% else %}
            <div class="frame" style="--n: 7; --d: 10; background-color: var(--color-surface);">
              <span class="font-bold text-brand text-2xl">
                {{ movie.data.title | truncate(1, true, "") }}
              </span>
            </div>
          {% endif %}
          <p class="meta">{{ movie.data.director }}</p>
          <div class="mt-2">
            <h3 class="text-sm line-clamp-2">
              {{ movie.data.title }}
            </h3>
            {% if movie.data.rating %}
              <p class="cluster text-xs" style="--gutter: var(--space-3xs); color: var(--color-star);">
                {% for i in range(1, 6) %}
                  {% if i <= movie.data.rating %}{% faIcon 'star', 'solid', '0.75em' %}{% endif %}
                {% endfor %}
              </p>
            {% endif %}
          </div>
        </a>
      </article>
    {% endfor %}
  </div>
  {% else %}
    <p>Nenhum filme registrado ainda.</p>
  {% endif %}
</section>
{% endif %}

{% if recentShows.length > 0 %}
<section class="region" style="--region-space: var(--space-2xl)" aria-labelledby="shows-title">
  <div class="cluster mb-6" style="--gutter: var(--space-xs)">
    <h2 id="shows-title">
      {% faIcon 'tv', 'solid', '1em' %} Séries Recentes
    </h2>
    <a href="/watching/shows/" class="text-sm" style="color: var(--color-link)">Ver todos →</a>
  </div>

  {% if recentShows.length > 0 %}
  <div class="grid h-feed" style="--grid-min-item-size: 12rem; --gutter: var(--space-xs)">
    {% for show in recentShows %}
      <article class="mb-5 h-entry">
        <a href="{{ show.url }}" class="block">
          {% if show.data.poster %}
            <div class="frame" style="--n: 7; --d: 10;">
              <img
                src="{{ show.data.poster | localPoster }}"
                alt=""
                loading="lazy"
              >
            </div>
          {% else %}
            <div class="frame" style="--n: 7; --d: 10; background-color: var(--color-surface);">
              <span class="font-bold text-brand text-2xl">{{ show.data.title | truncate(1, true, "") }}</span>
            </div>
          {% endif %}
          <div class="py-2">
            <h3 class="text-sm line-clamp-2">
              {{ show.data.title }}
            </h3>
            {% if show.data.rating %}
              <p class="cluster text-xs" style="--gutter: var(--space-3xs); color: var(--color-star);">
                {% for i in range(1, 6) %}
                  {% if i <= show.data.rating %}{% faIcon 'star', 'solid', '0.75em' %}{% endif %}
                {% endfor %}
              </p>
            {% endif %}
          </div>
        </a>
      </article>
    {% endfor %}
  </div>
  {% else %}
    <p>Nenhum seriado registrado ainda.</p>
  {% endif %}
</section>
{% endif %}

{% if favoriteMovies.length > 0 %}
<section class="region" style="--region-space: var(--space-2xl)" aria-labelledby="favorite-movies">
  <div class="cluster mb-6" style="--gutter: var(--space-xs)" id="favorite-movies">
    <h2>
      {% faIcon 'star', 'solid', '1em' %} Filmes Favoritos
    </h2>
  </div>

  {% if favoriteMovies.length > 0 %}
  <div class="grid h-feed" style="--grid-min-item-size: 12rem; --gutter: var(--space-xs)">
    {% for movie in favoriteMovies %}
      <article class="mb-5 h-entry">
        <a href="{{ movie.url }}" class="block">
          {% if movie.data.poster %}
            <div class="frame" style="--n: 7; --d: 10;">
              <img
                src="{{ movie.data.poster | localPoster }}"
                alt=""
                loading="lazy"
              >
            </div>
          {% else %}
            <div class="frame" style="--n: 7; --d: 10; background-color: var(--color-surface);">
              <span class="font-bold text-brand text-2xl">
                {{ movie.data.title | truncate(1, true, "") }}
              </span>
            </div>
          {% endif %}
          <div class="mt-2">
            <h3 class="text-sm line-clamp-2">
              {{ movie.data.title }}
            </h3>
            {% if movie.data.rating %}
              <p class="cluster text-xs" style="--gutter: var(--space-3xs); color: var(--color-star);">
                {% for i in range(1, 6) %}
                  {% if i <= movie.data.rating %}{% faIcon 'star', 'solid', '0.75em' %}{% endif %}
                {% endfor %}
              </p>
            {% endif %}
          </div>
        </a>
      </article>
    {% endfor %}
  </div>
  {% else %}
    <p>
      Nenhum filme favorito ainda.
    </p>
  {% endif %}
</section>
{% endif %}

{% if favoriteShows.length > 0 %}
<section class="region" style="--region-space: var(--space-2xl)" aria-labelledby="favorite-shows">
  <div class="cluster mb-6" style="--gutter: var(--space-xs)">
    <h2 id="favorite-shows">
      {% faIcon 'star', 'solid', '1em' %} Séries Favoritas
    </h2>
  </div>

  {% if favoriteShows.length > 0 %}
  <div class="grid h-feed" style="--grid-min-item-size: 12rem; --gutter: var(--space-xs)">
    {% for show in favoriteShows %}
      <article class="mb-5 h-entry">
        <a href="{{ show.url }}" class="block">
          {% if show.data.poster %}
            <div class="frame" style="--n: 7; --d: 10;">
              <img
                src="{{ show.data.poster | localPoster }}"
                alt=""
                loading="lazy"
              >
            </div>
          {% else %}
            <div class="frame" style="--n: 7; --d: 10; background-color: var(--color-surface);">
              <span class="font-bold text-brand text-2xl">
                {{ show.data.title | truncate(1, true, "") }}
              </span>
            </div>
          {% endif %}
          <div class="mt-2">
            <h3 class="text-sm line-clamp-2">
              {{ show.data.title }}
            </h3>
            {% if show.data.rating %}
              <p class="cluster text-xs" style="--gutter: var(--space-3xs); color: var(--color-star);">
                {% for i in range(1, 6) %}
                  {% if i <= show.data.rating %}{% faIcon 'star', 'solid', '0.75em' %}{% endif %}
                {% endfor %}
              </p>
            {% endif %}
          </div>
        </a>
      </article>
    {% endfor %}
  </div>
  {% else %}
    <p>Nenhuma série favorita ainda.</p>
  {% endif %}
</section>
{% endif %}

{% if yearMovies.length == 0 and yearShows.length == 0 %}
<p class="py-12 text-center">
  Nenhum filme ou série registrado para {{ year }}.
</p>
{% endif %}
