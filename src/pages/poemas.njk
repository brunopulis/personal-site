---
layout: poem
title: Poemas
description: "Coleção de poemas que escrevi ao longo dos anos"
permalink: "{% if pagination.pageNumber > 0 %}/poemas/page/{{ pagination.pageNumber + 1 }}/{% else %}/poemas/{% endif %}"
date: Last Modified
pagination:
  data: collections.poems
  size: 10
---

<div class="h-feed region flow">
  {% set items = pagination.items | default([]) %}
  {% for item in items %}
    <article class="card">
      <div class="flow">
        <h2 class="font-bold text-brand-red-500 text-2xl"><a href="{{ item.url }}">{{ item.data.title }}</a></h2>
        {% if item.date %}
          <time class="dt-published" datetime="{{ item.date | toISOString }}">
            {{ item.date | readableDate }}
          </time>
        {% endif %}
      </div>
    </article>
  {% endfor %}
  {% if pagination.pages.length > 1 %}
  {% include 'components/pagination.njk' %}
  {% endif %}
</div>