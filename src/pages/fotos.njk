---
layout: page
title: Fotos
description: Minhas fotos publicadas no Pixelfed
permalink: /fotos/
---

<header>
  <h1>Fotos</h1>
  <p>Fotos publicadas no <a href="https://pixelfed.social/brunopulis" rel="me noopener" target="_blank">Pixelfed</a>.
  </p>
</header>

{% if pixelfed and pixelfed.length > 0 %}
<div class="photo-grid">
  {% for photo in pixelfed %}
  <a href="{{ photo.postUrl }}" target="_blank" rel="noopener" class="photo-item" aria-label="Ver foto no Pixelfed">
    <img src="{{ photo.preview }}" alt="{{ photo.alt }}" loading="lazy" decoding="async" width="400" height="400">
  </a>
  {% endfor %}
</div>

<p class="photo-footer">
  <a href="https://pixelfed.social/brunopulis" rel="me noopener" target="_blank">Ver todas as fotos no Pixelfed →</a>
</p>

{% else %}
<p>Nenhuma foto encontrada no momento. Tente novamente mais tarde.</p>
{% endif %}

<style>
  .photo-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    gap: 1rem;
    margin-block: 2rem;
  }

  .photo-item {
    display: block;
    overflow: hidden;
    border-radius: var(--pico-border-radius, 0.375rem);
    aspect-ratio: 1;
  }

  .photo-item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: opacity 0.2s ease;
  }

  .photo-item:hover img {
    opacity: 0.85;
  }

  .photo-footer {
    text-align: center;
    margin-block-start: 1rem;
  }
</style>