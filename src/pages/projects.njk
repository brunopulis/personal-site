---
layout: page
title: "Meus projetos"
permalink: "/projects/index.html"
key: 'projects'
description: "Projetos Open Source que mantenho"
---


<section class="region">
  <div class="flow">
    <div class="grid" data-layout="thirds">
      {% for project in projects %}
      <article class="card flow">
        <h3>{{ project.title }}</h3>
        <p>{{ project.description }}</p>
        <a href="{{ project.url }}" target="_blank" rel="noopener noreferrer">Ver projeto <span
            class="visually-hidden">{{ project.title }}</span></a>
      </article>
      {% endfor %}
    </div>
  </div>
</section>