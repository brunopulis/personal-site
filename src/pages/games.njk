---
layout: page
title: "Meus jogos favoritos"
permalink: "{% if pagination.pageNumber > 0 %}/jogos/{{ pagination.pageNumber + 1 }}/{% else %}/jogos/{% endif %}"
description: "Coleção dos meus jogos favoritos"
pagination:
  data: collections.games
  size: 12
---

<header class="flow">
  <h1 class="p-name">{{ title }}</h1>
  <p>{{ description }}</p>
</header>

<ul class="grid" role="list">
  {% for game in games %}
    <article class="game-card">
      <img src="{{ game.coverImage }}" alt="" loading="lazy">

      <div class="game-content">
        <h2>{{ game.title }}</h2>

        <div class="game-meta">
          <span class="rating">⭐ {{ game.rating }}/5</span>
          {% if game.metacritic %}
          <span class="metacritic">Metacritic: {{ game.metacritic }}</span>
          {% endif %}
          <span class="release-date">{{ game.releaseDate | formatDate }}</span>
        </div>

        <div class="genres">
          {% for genre in game.genres %}
          <span class="genre-tag">{{ genre }}</span>
          {% endfor %}
        </div>

        <p class="description">{{ game.description | truncate(200) }}</p>

        <div class="platforms">
          <strong>Plataformas:</strong>
          {{ game.platforms | join(', ') }}
        </div>

        {% if game.website %}
        <a href="{{ game.website }}" class="game-link" target="_blank">
          Site Oficial →
        </a>
        {% endif %}
      </div>
    </article>
    {% endfor %}
  </div>
</div>
