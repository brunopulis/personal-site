---
layout: base
pagination:
  data: collections.notes
  size: 10
  alias: notes
permalink: '/notes/{% if pagination.pageNumber > 0 %}page/{{ pagination.pageNumber + 1 }}/{% endif %}'
title: Notas
---

<div class="h-feed">
  <h1 class="p-name">Notas</h1>
  <a class="visually-hidden u-url" href="{{ site.url }}/notes/"></a>
  <p>
    Ideias curtas e fáceis de conectar. Cada nota tem seu próprio link permanente.
  </p>

  <section class="flow" style="--flow-space: var(--space-l)">
    {% for note in notes %}
      <article class="callout flow h-entry" style="background-color: var(--color-light-silver-100); border-color: var(--color-light-silver-100); border-style: solid; --flow-space: var(--space-s);">
        <time class="meta dt-published" datetime="{{ note.date | htmlDateString }}">
          {{ note.date | readableDate }}
        </time>

        <div class="e-content prose">
          {{ note.content | safe }}
        </div>
      </article>
    {% endfor %}
  </section>

  <nav class="repel region" style="--region-space: var(--space-l)" aria-label="Pagination">
    {% if pagination.href.previous %}
      <a class="button" href="{{ pagination.href.previous }}">Anterior</a>
    {% else %}
      <span></span>
    {% endif %}
    {% if pagination.href.next %}
      <a class="button" href="{{ pagination.href.next }}">Próximo</a>
    {% endif %}
  </nav>
</div>
