export const htmlConfig = eleventyConfig => {
  // HTML minification disabled due to conflicts with eleventy-img picture elements
  // eleventyConfig.addTransform('html-minify', (content, path) => { ... });
};
